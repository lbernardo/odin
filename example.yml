create:
  directories: # Create directories for project
   $project/cmd
   $project/internal
   $project/internal/primary
   $project/internal/secondary
   $project/pkg
   $project/pkg/handler
   $project/pkg/models
commands:    
    interface:
      args:
        name : "Name of interface"
      copy:
        $resource/interface.temp: $project/adapters/$name


